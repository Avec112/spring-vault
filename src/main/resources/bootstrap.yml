# defaults to application
#spring.application.name: another-secret
spring:
  cloud:
    vault:
      token: 12345
      host: localhost
      port: 8200
      scheme: http
#      uri: http://localhost:8200


logging:
  level:
   root: WARN
   io.avec: DEBUG